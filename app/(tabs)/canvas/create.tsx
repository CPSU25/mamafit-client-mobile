import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from '~/components/ui/text'

export default function CreateCanvasScreen() {
  return (
    <SafeAreaView className='flex-1'>
      <Text>CreateCanvasScreen</Text>
    </SafeAreaView>
  )
}
