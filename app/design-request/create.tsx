import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from '~/components/ui/text'

export default function CreateDesignRequest() {
  return (
    <SafeAreaView className='flex-1'>
      <Text>CreateDesignRequest</Text>
    </SafeAreaView>
  )
}
